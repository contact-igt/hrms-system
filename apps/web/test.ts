import { platformApi } from "./src/features/platform/api/platform.api";

// We need an access token, but we can't easily get it.
// Let's just create a test login script, or just check the code again.
